const resume_card = document.querySelectorAll('.resume_card');
const resume_main = document.querySelector('.resume_main');
/*
resume_card.forEach(function (item,index) {
    item.onclick = function () {
        resume_main.style.display = 'none';
        let index = item.getAttribute('data-set');
        console.log(index);
    }
});*/
